rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Curio clients consume feed content through getCurioFeed. The Admin SDK
    // used by Cloud Functions bypasses these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
